apiVersion: v1
kind: PersistentVolume
metadata:
  name: stag-postgresql-pv
spec:
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: gp2
